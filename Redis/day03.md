# 如何保证数据库和缓存的一致性  
### Cache Aside
先从缓存中读取数据，如果没有就再去数据库里面读数据，然后把数据放回缓存中，
如果缓存中可以找到数据就直接返回数据；更新数据的时候先把数据持久化到数据库，然后再让缓存失效。
### 延迟双删 
```
更新数据库
删除缓存
sleep(100ms)  
再删除缓存
``` 
### 消息队列保证最终一致性  
```
更新数据库
↓
发送MQ消息
↓
消费者删除缓存
```
### 数据库 binlog 同步缓存    
```
监听数据库 binlog：

数据库更新 → 自动删除 Redis
canal 工具
```

